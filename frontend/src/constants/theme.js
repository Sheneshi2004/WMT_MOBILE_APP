import { COLORS, STATUS_COLORS, SPACING, BORDER_RADIUS, TYPOGRAPHY } from './colors';

export const theme = {
  colors: COLORS,
  statusColors: STATUS_COLORS,
  spacing: SPACING,
  borderRadius: BORDER_RADIUS,
  typography: TYPOGRAPHY,
};

// Export everything for easier imports
export { COLORS, STATUS_COLORS, SPACING, BORDER_RADIUS, TYPOGRAPHY };
export default theme;